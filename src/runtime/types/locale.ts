export type { Locale, LocaleDirection } from '@byyuurin/ui-kit'

export interface Messages {
  alert: {
    close: string
  }
  calendar: {
    prevYear: string
    nextYear: string
    prevMonth: string
    nextMonth: string
  }
  carousel: {
    dots: string
    prev: string
    next: string
    goto: string
  }
  inputNumber: {
    increment: string
    decrement: string
  }
  modal: {
    close: string
  }
  table: {
    noData: string
  }
  toast: {
    close: string
  }
}
