export interface Messages {
  alert: {
    close: string
  }
  calendar: {
    prevYear: string
    nextYear: string
    prevMonth: string
    nextMonth: string
  }
  carousel: {
    prev: string
    next: string
    goto: string
  }
  inputNumber: {
    increment: string
    decrement: string
  }
  modal: {
    close: string
  }
  table: {
    noData: string
  }
  toast: {
    close: string
  }
}
