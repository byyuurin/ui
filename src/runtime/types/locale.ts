export interface Messages {
  alert: {
    close: string
  }
  carousel: {
    prev: string
    next: string
    goto: string
  }
  inputNumber: {
    increment: string
    decrement: string
  }
  modal: {
    close: string
  }
  table: {
    noData: string
  }
  toast: {
    close: string
  }
}
